# Docker Environment Variables
# Copy this file to .env and update with your secure passwords

# Database Password for Docker Compose
DB_PASSWORD=your_secure_password_here

# Instructions:
# 1. Copy this file: cp docker.env.example .env
# 2. Change DB_PASSWORD to a strong, unique password
# 3. Never commit .env to version control
# 4. Use this for Docker Compose: docker-compose up -d
