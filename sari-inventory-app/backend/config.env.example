# Database Configuration
# Copy this file to config.env and update with your database details

# PostgreSQL Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=MH Factory Sari-Tracking
DB_USER=postgres
DB_PASSWORD=your_password_here

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: Database Connection Pool Settings
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# Instructions:
# 1. Copy this file: cp config.env.example config.env
# 2. Update the values above with your actual database credentials
# 3. Make sure your PostgreSQL database is running
# 4. Create the database: CREATE DATABASE "MH Factory Sari-Tracking";
# 5. Start the server: node server.js
